# android-parent

Optional native parent app for Android. Ships in v1. For most users the [parent-web](../parent-web) PWA wrapped in a Capacitor shell is enough; build a fully native app here only if a need appears (deeper notification handling, widgets). Shares the API contract in [../../packages/proto/openapi.yaml](../../packages/proto/openapi.yaml).
